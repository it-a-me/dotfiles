sudo doas
mpv-term mpv --vo=kitty --vo-kitty-use-shm=yes
ls ls --color=auto
zj zellij

vim nvim -p

dotman git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME
#dotman-setup ( test -d "$HOME/.dotfiles" || ( git clone --bare https://github.com/it-a-me/dotfiles ~/.dotfiles && dotman checkout ) ) && dotman config status.showUntrackedFiles no
mu-dlp yt-dlp -f bestaudio --extract-audio --audio-format mp3 --ignore-config --embed-thumbnail --output '%(title)s.%(ext)s'
show_path echo $PATH | tr ":" "\n"
g git

wm-ctl just --justfile ~/.config/window_manager/justfile --working-directory ~/.config/window_manager
